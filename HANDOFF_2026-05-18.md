# Agent Passport Handoff - 2026-05-18

## Read This First

Active repo:

```text
/Users/obiihej/dev/agent-passport
```

Active branch:

```text
main
```

Remote:

```text
git@github.com:UseOrchestrator/agent-passport.git
```

Do not resume Agent Passport work from `/Users/obiihej/dev/orchestrator` unless the task is explicitly about monorepo docs. The Agent Passport product/code source of truth is the standalone repo above.

## Current State

Before this checkpoint, local `main` was 4 commits ahead of `origin/main`:

```text
763a3ed Add real connection profiles to demo
0b7e107 Replace fake chat planner with model tool loop
d10e092 Make chat demo use model planning
94eb1b7 Add chat agent demo
```

This checkpoint adds the in-progress full-demo work around selected profile startup context.

## What Was In Progress

Files in progress:

```text
examples/full-demo/public/index.html
examples/full-demo/src/agent.ts
```

Intent:

- Make the full demo show the selected Agent Passport profile context before the model runs.
- Stop pretending there is always a tool call just because a passport was read.
- Only show provider tool calls when the agent actually calls provider tools.
- Pass selected profile startup context through the chat API result so the UI can display exactly what the model saw.
- Replace hardcoded "Default passport" language with the selected profile name.

## Verification

Last verified command:

```text
yarn typecheck
```

Result:

```text
passed
```

Only warning seen was Node's `url.parse()` deprecation warning from dependencies.

## Next Steps

1. Reopen `/Users/obiihej/dev/agent-passport`.
2. Run `git status --short --branch` and confirm this WIP checkpoint is present.
3. Run `yarn typecheck`.
4. Start the full demo with `yarn demo:full` and inspect the UI.
5. Verify profile switching updates the startup context.
6. Verify normal chat answers without fake tool-call rows.
7. Verify app-use requests show real provider tool calls only when the model calls `execute_provider_readonly_tool`.
8. Decide whether to push `main` or move the ahead commits onto a PR branch first.

## Known Boundary

The Orchestrator monorepo at `/Users/obiihej/dev/orchestrator` is not the active Agent Passport worktree. It currently has unrelated mission-related local changes and should not be used for Agent Passport implementation.
